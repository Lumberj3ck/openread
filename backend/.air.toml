root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/openread ."
  bin = "./tmp/openread"
  include_ext = ["go"]
  exclude_dir = ["data", "tmp"]
  delay = 200
  stop_on_error = true
  send_interrupt = true

[screen]
  clear_on_rebuild = true
